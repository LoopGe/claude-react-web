{
  "name": "quota-monitor-plugin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/service.ts --bundle --platform=node --format=esm --outfile=dist/service.mjs --banner:js=\"import { createRequire } from 'node:module';const require=createRequire(import.meta.url);\""
  }
}